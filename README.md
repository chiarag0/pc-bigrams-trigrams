This project contains a sequential and parallel implementation of character-level and word-level n-gram histogram computation. 
The sequential algorithm is parallelized using OpenMP and can be run with multiple scheduling policies (static, dynamic, guided) and chunk sizes (auto, 64, 512, 4096). 
